{% extends 'base.html.twig' %}
{% block title %}Register{% endblock %}
{% block body %}
    <div class="menu-bar">
        {{ include ('page/partials/navigator.html.twig') }}
    </div>
    <div class="body-content">
        <header id="headerImg">
            <img src="img/frutiger-aero.png" alt="titulo">
            <img src="img/welcomeFA.png" alt="welcomeFA">
        </header>
        <div id="contenido-register">
            <h1>Regístrate</h1>

            <p>Regístrate, si eres nuevo. </p>
            <p>Asegúrate antes de haber iniciado sesión. Si ya la iniciaste, no hace falta que te registres.</p>
            <p>Dale a <i>"Iniciar sesión"</i> para entrar con tu cuenta.</p>
            {{ form_start(registrationForm) }}
            <p>
                {{ form_row(registrationForm.name, {
                    label: 'Nombre Completo'
                }) }}
            </p>
            <p>
                {{ form_row(registrationForm.email, {
                    label: 'Email / Correo electrónico'
                }) }}
            </p>
            <p>
                {{ form_row(registrationForm.plainPassword, {
                    label: 'Contraseña'
                }) }}
            </p>
            <p>
                {{ form_row(registrationForm.agreeTerms, {
                    label: 'Estoy de acuerdo con los terminos y condiciones del servicio'
                }) }}
            </p>
            <button type="submit" class="btn">Registrarse</button>
            {{ form_end(registrationForm) }}

            {{ form_errors(registrationForm) }}
        </div>
    </div>
{% endblock %}
